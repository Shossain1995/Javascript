// 5. Find the number Even or Odd.
// // Algorithm:
// step:set variable 
// set logic var%2==0
// if 0 it Evan or not 0 than odd.

var num=parseInt(prompt("Enter The Number"));

if(num%2==0){
     document.write(num+ "  is a Even Number");
}else{
     document.write(num+ "  is a odd number");
}